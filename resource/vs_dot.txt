#version 450
#pragma shader_stage(vertex)
layout(location=0) in vec3 position;
layout(location=1) in int show;

layout(std140, binding=0) uniform UserData {
	mat4 vp;
} data;

void main()
{
	if(show==1)
		gl_Position=data.vp*vec4(position, 1.0);
	else
		gl_Position=vec4(0, 0, -2, 1.0);
}